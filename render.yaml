services:
  - type: web
    name: reikage-watch
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
      - key: JWT_SECRET
        value: reikage_black_tier_stable_auth_key_2026_reikage_watch
